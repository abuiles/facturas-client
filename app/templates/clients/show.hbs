<div class="row">
  {{confirmable-action text='Remover' confirmAction='destroyRecord'}}
  <h3>{{#link-to 'clients.edit' this}}Editar{{/link-to}}</h3>
  <h3>{{model.fullName}}</h3>
  <h3>{{model.phone}}</h3>
</div>
<div class="row">
  {{outlet "invoice-items"}}
</div>
