import {
  emberList
} from '../../../helpers/ember-list';
import { module, test } from 'qunit';

module('EmberListHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = emberList(42);
  assert.ok(result);
});
