import {
  emberVirtualList
} from '../../../helpers/ember-virtual-list';
import { module, test } from 'qunit';

module('EmberVirtualListHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = emberVirtualList(42);
  assert.ok(result);
});
